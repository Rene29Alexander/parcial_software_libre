Nombre de grupo: Teams playstation
Integrantes:
Rene Alexander Araujo Soto
Wilber Jose Jimenez Ramirez
Jonathan Elias gamez Larin
----------Comandos---------------


cd /home #Entramos al directorio home
mkdir UGB #Creamos el directorio UGB dentro de home
cd UGB #Entramos al directorio UGB
mkdir 2024# Creamos el directorio 2024
cd 2024# Entramos al directorio 2024
mkdir CicloI# Creamos el directorio CicloI
mkdir CicloII# Creamos el directorio CicloII
cd CicloII# Entramos al directorio CicloII
mkdir SoftwareLibre# Creamos el directorio SoftwareLibre
cd SoftwareLibre# Entramos al directorio SoftwareLibre
mkdir teams_playstation# Se crea el directorio teams_playstation dentro de SoftwareLibre
mkdir nano integrantes.txt #Abre editor de texto nano y se crea el archivo integrantes.txt con el nombre del grupo y los integrantes

----------------PARTE 3---------------------

cat /etc/apt/sources.list #Abre el archivo sources.list donde estan las rutas de los repositorios
apt update #Busca actualizaciones disponibles
apt upgrade #Se actualizan los repositorios de la maquina
apt install Prometheus #instala el servicio Prometheus
apt install Samba #Instala el servicio Samba
apt install docker.io #Instala el servicio docker.io
systemctl mask docker #Deshabilita el servicio por completo
systemctl list-unit-files --type service -all #Muestra la lista de todos los servicios instalados en la maquina con sus estados
cd /home/UGB #Entramos a la carpeta UGB
tree #Muestra la estructura de arbol del directorio
cd 2024/UGB/cicloII/SoftwareLibre/teams_playstation   #Entramos en el directorio teams_playstation
cat Integrantes.txt   #Muestra todo el contenido del archivo de texto.

